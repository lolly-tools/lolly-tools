{
  "id": "logo-wall",
  "name": "Logo Wall",
  "description": "Arrange a pile of logos into a clean, even sponsor grid - the “NASCAR” wall.",
  "version": "1.4.2",
  "engineVersion": "^1.0.0",
  "status": "official",
  "category": "designer",
  "tags": ["logo", "lockup", "wall", "grid", "nascar", "logos", "brand", "partners", "customers", "sponsors", "clients", "montage", "cloud", "vector", "threshold", "on-device"],
  "a11yLabel": "A grid of {{logos.length}} logos",
  "render": {
    "width": 1280,
    "height": 720,
    "formats": ["png", "svg", "penpot", "pdf", "dxf", "jpg", "webp", "avif", "tiff", "emf", "eps", "pdf-cmyk"],
    "actions": ["copy", "download", "save"],
    "layout": "sidebar",
    "transparentBg": true,
    "convertPaths": false
  },
  "hooks": {
    "onInit": true,
    "onInput": true,
    "beforeExport": true
  },
  "inputs": [
    {
      "id": "columns",
      "label": "Columns",
      "type": "number",
      "display": "slider",
      "min": 1,
      "max": 10,
      "step": 1,
      "default": 4,
      "help": "How many logos sit in each row."
    },
    {
      "id": "gap",
      "label": "Horizontal spacing",
      "type": "number",
      "display": "slider",
      "min": 0,
      "max": 160,
      "step": 1,
      "default": 32,
      "help": "Gap between logos across a row."
    },
    {
      "id": "gapY",
      "label": "Vertical spacing",
      "type": "number",
      "display": "slider",
      "min": 0,
      "max": 160,
      "step": 1,
      "default": 32,
      "help": "Gap between rows of logos."
    },
    {
      "id": "padding",
      "label": "Horizontal padding",
      "type": "number",
      "display": "slider",
      "min": 0,
      "max": 200,
      "step": 1,
      "default": 48,
      "help": "Breathing room on the left and right of the wall."
    },
    {
      "id": "paddingY",
      "label": "Vertical padding",
      "type": "number",
      "display": "slider",
      "min": 0,
      "max": 200,
      "step": 1,
      "default": 48,
      "help": "Breathing room above and below the wall."
    },
    {
      "id": "background",
      "label": "Background",
      "type": "color",
      "default": "{color.semantic.surface}"
    },
    {
      "id": "balance",
      "label": "Balance sizes",
      "type": "boolean",
      "default": true,
      "help": "Auto-size each logo by its optical weight - heavier logos shrink, lighter ones grow - so the wall reads as evenly weighted. Each logo's own Size % still applies on top. Turn off for uniform sizing."
    },
    {
      "id": "trim",
      "label": "Trim margins",
      "type": "boolean",
      "default": true,
      "help": "Crop each logo to its artwork - transparent or flat-colour margins are measured away (SVGs included, still fully vector) so every logo fits its cell by its real content. Turn off if a logo's clear-space padding is deliberate."
    },
    {
      "id": "knockout",
      "label": "Remove white backgrounds",
      "type": "boolean",
      "default": true,
      "help": "A logo lifted from an old slide or screenshot usually arrives in a solid white box. This makes that near-white backing transparent so the logo sits directly on your wall - coloured tiles and white inside the mark itself are kept. Needs Trim margins on."
    },
    {
      "id": "vectorizeAll",
      "label": "Render all as vector",
      "type": "boolean",
      "display": "pill",
      "default": false,
      "section": "Vector ink",
      "help": "Flatten the whole wall to the single ink colour below. SVG logos are recoloured losslessly; image logos are traced to crisp paths. Each logo can also opt in individually in its block."
    },
    {
      "id": "inkColor",
      "label": "Ink colour",
      "type": "color",
      "default": "{color.semantic.primary}",
      "section": "Vector ink",
      "help": "The single ink for any logo rendered as vector."
    },
    {
      "id": "threshold",
      "label": "Threshold",
      "type": "number",
      "display": "slider",
      "min": 1,
      "max": 99,
      "step": 1,
      "default": 55,
      "section": "Vector ink",
      "help": "Default brightness cut-off for traced logos. Higher keeps more of each logo; tune a finnicky one per logo in its block."
    },
    {
      "id": "invert",
      "label": "Invert",
      "type": "boolean",
      "default": false,
      "section": "Vector ink",
      "help": "Swap which side of the threshold becomes ink. White / pale logos are auto-inverted, so you rarely need this; tune per logo if one needs it."
    },
    {
      "id": "detail",
      "label": "Detail",
      "type": "number",
      "display": "slider",
      "min": 80,
      "max": 600,
      "step": 10,
      "default": 300,
      "section": "Vector ink",
      "help": "Minimum tracing resolution. The trace automatically samples finer than the source image where that gains precision; raise this only if fine shapes still drop out."
    },
    {
      "id": "smoothing",
      "label": "Smoothing",
      "type": "number",
      "display": "slider",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 60,
      "section": "Vector ink",
      "help": "How much traced outlines round into curves. Low keeps edges crisp; high flows them into smooth Bézier curves."
    },
    {
      "id": "repeat",
      "label": "Repeat",
      "type": "select",
      "default": "off",
      "display": "segmented",
      "section": "Step & repeat",
      "help": "Tile the whole logo set to fill a backdrop - the press-wall treatment behind interviews and event photos. Grid stacks rows squarely, Brick offsets alternate rows by half a cell, Checker staggers logos diagonally with space between.",
      "options": [
        { "value": "off", "label": "Off" },
        { "value": "grid", "label": "Grid" },
        { "value": "brick", "label": "Brick" },
        { "value": "checker", "label": "Checker" }
      ]
    },
    {
      "id": "repeatRows",
      "label": "Rows",
      "type": "number",
      "display": "slider",
      "min": 2,
      "max": 16,
      "step": 1,
      "default": 6,
      "section": "Step & repeat",
      "showIf": { "repeat": ["grid", "brick", "checker"] },
      "help": "How many rows the repeated wall stacks. Logos keep cycling in order, so neighbouring repeats stay varied."
    },
    {
      "id": "logos",
      "label": "Logos",
      "type": "blocks",
      "labelledFields": true,
      "dropToAdd": { "field": "logo", "accept": "image/*" },
      "fields": [
        { "id": "logo", "label": "Image", "type": "asset", "assetType": "any", "allowUpload": true },
        { "id": "presence", "label": "Presence", "type": "select", "default": "normal",
          "help": "How prominent this logo is. Balancing equalises everyone first; Presence then deliberately weights this one up or down - sponsor-tier sizing.",
          "options": [
            { "value": "hero", "label": "Hero" },
            { "value": "large", "label": "Large" },
            { "value": "normal", "label": "Normal" },
            { "value": "small", "label": "Small" }
          ] },
        { "id": "scale", "label": "Size %", "type": "number", "display": "slider", "min": 25, "max": 300, "step": 5, "default": 100, "help": "Fine nudge on top of Presence and balancing." },
        { "id": "opacity", "label": "Opacity", "type": "number", "display": "slider", "min": 0, "max": 1, "step": 0.05, "default": 1 },
        { "id": "filter", "label": "Filter", "type": "select", "default": "none", "options": [
          { "value": "none", "label": "None" },
          { "value": "grayscale", "label": "Grayscale" }
        ] },
        { "id": "vectorize", "label": "Render as vector", "type": "boolean", "default": false,
          "help": "Flatten this logo to the single ink colour (crisp vector). Off keeps it as its original image." },
        { "id": "tune", "label": "Tune this logo", "type": "boolean", "default": false, "showIf": { "vectorize": true },
          "help": "Give this logo its own vector settings - for finnicky marks that need a different threshold or inverting." },
        { "id": "threshold", "label": "Threshold", "type": "number", "display": "slider", "min": 1, "max": 99, "step": 1, "default": 55, "showIf": { "vectorize": true, "tune": true },
          "help": "Brightness cut-off for this logo. Higher keeps more of it." },
        { "id": "invert", "label": "Invert", "type": "boolean", "default": false, "showIf": { "vectorize": true, "tune": true },
          "help": "For a white or pale logo." },
        { "id": "detail", "label": "Detail", "type": "number", "display": "slider", "min": 80, "max": 320, "step": 10, "default": 200, "showIf": { "vectorize": true, "tune": true } },
        { "id": "smoothing", "label": "Smoothing", "type": "number", "display": "slider", "min": 0, "max": 100, "step": 5, "default": 60, "showIf": { "vectorize": true, "tune": true } }
      ],
      "default": []
    }
  ]
}
